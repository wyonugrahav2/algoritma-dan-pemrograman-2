[project]
name = "modul-09-architecting-robust-software"
version = "0.1.0"
description = "Fault-Tolerant CLI Engine & Resilience Inspector"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
