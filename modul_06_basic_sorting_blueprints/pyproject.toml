[project]
name = "modul-06-basic-sorting-blueprints"
version = "1.0.0"
description = "CLI Data Sorting Engine & Mutation Visualizer - Modul 06 ALGO2"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
